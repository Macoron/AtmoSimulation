using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Unity.Mathematics;

public class GridTest : MonoBehaviour
{
    /*private ChunkedGrid<float> grid;

    // Start is called before the first frame update
    void Start()
    {
        grid = new ChunkedGrid<float>(3, 10);

        grid.AddChunk(new int2(0, 0));

        for (int x = 0; x < grid.chunkSize; x++)
        {
            for (int y = 0; y < grid.chunkSize; y++)
            {
                grid[x, y] = UnityEngine.Random.Range(-1f, 1f);
            }
        }
        print(grid[0, 0]);

        grid.AddChunk(new int2(-1, -1));
        grid[-1, -1] = 3f;
        grid[-2, -1] = 2f;

        grid.AddChunk(new int2(0, -1));
        grid[0, -1] = 3f;
    }

    private void OnDestroy()
    {
        grid.Dispose();
    }

    // Update is called once per frame
    void Update()
    {

    }*/
}
